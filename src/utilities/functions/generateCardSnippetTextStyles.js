export function cardTextClass(title) {
  if (title === 'code') return '-top-36 -left-32';
  if (title === 'figma') return '-top-[8.25rem] -left-[20.5rem]';
  if (title === 'website') return 'top-[5.5rem] -left-[20.5rem]';
}
